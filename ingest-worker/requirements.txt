pika==1.3.2
firebase-admin==6.6.0
pymupdf==1.28.0
pymupdf4llm==1.28.0
python-docx==1.1.2
# Best-of-breed parse stage (issue #13): docx/html/xml + PDF-OCR fallback. Models
# are baked into the image at build time (see Dockerfile) so conversions run offline.
docling==2.114.0
# OCR fallback for PDFs with no text layer (Apache-2.0; onnxruntime CPU, offline).
# Docling 2.114's RapidOCR plugin imports the 3.x `rapidocr` package (`from rapidocr
# import EngineType, RapidOCR`), NOT the legacy `rapidocr_onnxruntime` — pin that.
# Pulls opencv-python -> needs libgl1/libglib2.0-0 in the image (see Dockerfile).
rapidocr==3.9.2
onnxruntime==1.20.1
