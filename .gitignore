# Firebase Admin credentials — never commit, anywhere in the tree
serviceAccountKey.json
**/serviceAccountKey.json
serviceAccount.json
**/serviceAccount.json

# Firebase emulator debug logs
*-debug.log
**/*-debug.log

# code-explainer HTML output (build artifacts, not source)
code-explanations/
